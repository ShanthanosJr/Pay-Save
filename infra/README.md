# infra

AWS CDK (TypeScript) or Terraform, added in Phase 8: VPC, RDS PostgreSQL 16 (PITR on), ElastiCache Redis, ECS Fargate services, Cognito user pool (phone sign-in), SQS/SNS, EventBridge Scheduler, S3 (+Object Lock bucket for ledger exports), CloudFront, KMS keys.
